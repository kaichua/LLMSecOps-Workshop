[project]
name = "llmsecops-workshop"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "fastapi",
    "pydantic",
    "tf-keras",
    "torch",
    "transformers",
    "uvicorn",
]
